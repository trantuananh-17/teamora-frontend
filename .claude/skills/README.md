# Skills — teamora-frontend

## Skill của Teamora

| Skill | Bao gồm |
|---|---|
| `teamora-frontend` | Tầng lấy dữ liệu, gate, `event.status` chi phối màn hình CBNV, My Journey, form, bảng admin |
| `teamora-ui` | Token màu, component dùng chung, hiển thị dữ liệu nghiệp vụ, trạng thái rỗng, responsive |

Sửa thoải mái khi thực tế đổi. Chúng dẫn chiếu về `../rules/` và `../../.claude/docs/` cho phần sự
thật, không lặp lại nội dung ở đó.

## Skill vendor — **không sửa tại chỗ**

`shadcn/` — copy nguyên văn từ `D:\Learn_code\Ragenta\.claude\skills\shadcn\` ngày 2026-09-10
(gốc: shadcn/ui).

**Không phải của chúng ta.** Sửa tại chỗ là mất lặng lẽ ở lần refresh tiếp theo từ upstream, và làm
nguồn gốc của nó thành một lời nói dối. Teamora cần hướng dẫn khác thì viết vào `teamora-ui` bên
cạnh và nói rõ nó khác ở đâu, vì sao.

Ragenta còn có `migrate-radix-to-base` — **cố ý không lấy**. Teamora bắt đầu thẳng trên
`radix-nova`, không có gì để migrate.
